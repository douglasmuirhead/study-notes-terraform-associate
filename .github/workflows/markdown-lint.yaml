name: Markdown Lint
on:
  pull_request:

jobs:
  runs-on: [linux]
  steps:

    - name: Checkout
      uses: actions/checkout@v4
  
    - uses: articulate/actions-markdownlint@v1
       with:
         config: markdownlint-config.json
         files: '*.md'
         version: 0.28.1